# Migraine Weatherr — handoff (2026-06-12)

Continuing the `feature/menstrual-cycle-trigger` branch. Spec: `docs/superpowers/specs/2026-06-12-cycle-tracking-design.md`. Plan: `~/.claude/plans/start-the-menustral-cycle-concurrent-alpaca.md`.

> ⚠️ Pre-existing dirty state on this branch when work started: `android/app/build.gradle.kts`, `pubspec.yaml` (`workmanager: ^0.5.2` → `^0.9.0`), `pubspec.lock`. Untouched by this work — assume someone else's WIP.

## Goal

Add lightweight period logging (start/end + baseline severity, sparse per-day overrides) and surface derived cycle phase on the Insights heatmap (ribbon) and day-detail sheet. No correlation engine wiring (deferred per spec).

## Slice status

- [x] Slice 1 — Domain types + phase derivation (118 domain tests pass, +22 new)
- [x] Slice 2 — Drift schema v4 + JournalSource methods (Periods + PeriodDaySeverities tables, migration v3→v4, full source impl, watch* streams, 110 flutter tests pass)
- [x] Slice 3 — Providers (recent/current/dayPhase/effectiveDaySeverity, 6 tests pass)
- [x] Slice 4 — Home screen "Log/End period" button (label flips, dialog persists baseline, 120 flutter tests pass)
- [ ] Slice 5 — Insights phase ribbon
- [ ] Slice 6 — Day-detail cycle row
- [ ] Slice 7 — Heatmap day-tap mark-start/mark-end

## Pre-existing related code

- `packages/domain/lib/src/modules/menstrual_phase.dart` — *separate* trigger module sourced from HealthKit `menstrualHistory`. This work does **not** touch it. The spec's "correlation contributor" path is deferred; this work only adds user-logged periods + UI.

## Notes / pending

(filled in as slices land)
