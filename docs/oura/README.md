# Oura reference material

`openapi-1.34.json` is the Oura Ring API OpenAPI spec, version 1.34, kept here for reference. The app does not parse or import this file; it's the source of truth used when authoring `lib/data/models/oura_models.dart` and `lib/data/sources/oura_api_client.dart`.
